{
  "name": "mind-digger-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter web dev",
    "build": "bun --filter web build",
    "preview": "bun --filter web preview",
    "migrate": "bun run scripts/migrate.ts",
    "validate-migration": "bun run scripts/validate-migration.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
